#include <stdio.h>
#include <conio.h>
#include <time.h>

main(){
	printf("Reloj\n");
	int Ho, h, Mi, m, Se, s;
	printf("Inserte las horas: ");
	scanf("%d", h);
	printf("Inserte los minutos: ");
	scanf("%d", m);
	printf("Inserte los segundos: ");
	scanf("%d", s);
	for(Se = 0; Mi < m + 1 && Se < s + 1 && Ho < h + 1; Se++)
	{
		
	}
	getch();
}
